{
  "name": "epub-gen3",
  "version": "0.1.0",
  "description": "Generate EPUB files from Rust — Node.js native addon",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "epub-gen3",
    "triples": {
      "defaults": true,
      "additional": [
        "x86_64-unknown-linux-musl",
        "aarch64-unknown-linux-gnu",
        "aarch64-apple-darwin"
      ]
    }
  },
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "prepublishOnly": "napi prepublish -t npm"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0"
  },
  "files": [
    "index.js",
    "index.d.ts",
    "*.node"
  ],
  "license": "MIT",
  "optionalDependencies": {
    "epub-gen3-win32-x64-msvc": "0.1.0",
    "epub-gen3-darwin-x64": "0.1.0",
    "epub-gen3-linux-x64-gnu": "0.1.0",
    "epub-gen3-linux-x64-musl": "0.1.0",
    "epub-gen3-linux-arm64-gnu": "0.1.0",
    "epub-gen3-darwin-arm64": "0.1.0"
  }
}